# Optional: fallback credentials if you don't want to use macOS Keychain.
# Preferred setup is `beehiiv-mcp auth set` which writes to Keychain.
#
# Create an API key at https://app.beehiiv.com/settings/integrations/api
BEEHIIV_API_KEY=
BEEHIIV_PUBLICATION_ID=
